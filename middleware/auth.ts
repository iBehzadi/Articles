export default defineNuxtRouteMiddleware((to, from) => {
  // const isLoggedIn = false;


  // if (isLoggedIn) {
  //   //redirect to the page we want
  //   return navigateTo(to.fullPath);
  // }
  // //redirect to login page
  console.log('middleware');
  
  // return navigateTo("/login");
});
